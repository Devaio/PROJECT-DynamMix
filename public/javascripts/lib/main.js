// Generated by CoffeeScript 1.6.3
(function() {
  $(function() {
    $('.signUpSplash').on('submit', function(e) {
      e.preventDefault();
      console.log($(this).serialize());
      $.post("/signin", $(this).serialize(), function(data) {
        console.log(data);
      });
      $('#signInModal').modal('toggle');
    });
    $(document).on('submit', '.artistSearchForm', function(e) {
      var $relArtistsList, relArtTemplate;
      e.preventDefault();
      $.post('/related', $(this).serialize(), function(data) {
        console.log(data);
        $relArtistsList.html(relArtTemplate({
          artist: data.artist,
          relArt: data.relatedArtists
        }));
      });
      $relArtistsList = $('#relatedArtistListCont');
      relArtTemplate = Handlebars.compile($('#relArt-template').html());
      $('.artistSearchForm').val('');
    });
  });

}).call(this);
